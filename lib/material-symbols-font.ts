import localFont from "next/font/local";

/** Material Symbols Outlined — loaded via next/font so the woff2 resolves in Next.js. */
export const materialSymbolsOutlined = localFont({
  src: "../node_modules/material-symbols/material-symbols-outlined.woff2",
  display: "block",
});
